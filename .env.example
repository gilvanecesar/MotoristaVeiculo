# =================================================
# ARQUIVO .env.example - CONFIGURAÇÕES DO QUERO FRETES
# =================================================
# COPIE ESTE ARQUIVO PARA .env E CONFIGURE COM SUAS CREDENCIAIS REAIS
# NUNCA COMMIT O ARQUIVO .env NO GIT!

# =================================================
# CONFIGURAÇÕES DO BANCO DE DADOS
# =================================================
DATABASE_URL=postgresql://postgres:SUA_SENHA_POSTGRES_AQUI@postgres:5432/quero_fretes
POSTGRES_DB=quero_fretes
POSTGRES_USER=postgres
POSTGRES_PASSWORD=SUA_SENHA_POSTGRES_FORTE_AQUI
DB_PORT=5432

# =================================================
# CONFIGURAÇÕES DA APLICAÇÃO
# =================================================
NODE_ENV=production
PORT=5000

# =================================================
# CHAVES DA API - OBRIGATÓRIAS
# =================================================

# OpenAI (Assistente AI "Buzino")
# Obtenha em: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-proj-XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX

# Google Analytics (Rastreamento)
# Obtenha em: https://analytics.google.com/
VITE_GA_MEASUREMENT_ID=G-XXXXXXXXXX

# =================================================
# CONFIGURAÇÕES DE EMAIL
# =================================================

# Gmail (recomendado)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=seu.email@gmail.com
SMTP_PASS=sua_senha_de_app_gmail

# Ou Hostinger
# SMTP_HOST=smtp.hostinger.com
# SMTP_PORT=587
# SMTP_USER=seu.email@seudominio.com
# SMTP_PASS=sua_senha_email

# Ou outros provedores
# SMTP_HOST=smtp.seuprovedores.com
# SMTP_PORT=587
# SMTP_USER=seu.email@provedor.com
# SMTP_PASS=sua_senha

# =================================================
# SISTEMA DE PAGAMENTOS
# =================================================

# OpenPix (PIX Brasileiro) - OBRIGATÓRIO
# Obtenha em: https://openpix.com.br/
OPENPIX_APP_ID=sua_chave_openpix_aqui
OPENPIX_WEBHOOK_SECRET=seu_webhook_secret_openpix

# =================================================
# INTEGRAÇÕES EXTERNAS
# =================================================

# N8N Webhooks (Opcional - Automação)
N8N_WEBHOOK_URL=https://sua-instancia-n8n.com/webhook/usuario-criado

# WhatsApp Business API (Opcional)
WHATSAPP_TOKEN=seu_token_whatsapp_business
WHATSAPP_VERIFY_TOKEN=seu_token_verificacao

# =================================================
# APIs BRASILEIRAS
# =================================================

# ReceitaWS (Validação CNPJ) - Geralmente não precisa de chave
RECEITA_WS_API_URL=https://www.receitaws.com.br/v1

# IBGE (Cidades e Estados) - API pública
IBGE_API_URL=https://servicodados.ibge.gov.br/api/v1

# =================================================
# CONFIGURAÇÕES DE SEGURANÇA
# =================================================

# Chave secreta para JWT (gere uma chave forte)
JWT_SECRET=sua_chave_jwt_super_secreta_e_longa_aqui_123456

# Salt para hash de senhas
BCRYPT_SALT_ROUNDS=12

# =================================================
# CONFIGURAÇÕES OPCIONAIS
# =================================================

# Domínio da aplicação (produção)
APP_DOMAIN=localhost
APP_URL=http://localhost

# Configurações de CORS (se necessário)
CORS_ORIGIN=http://localhost,https://seudominio.com

# Timeout para requisições (segundos)
REQUEST_TIMEOUT=30

# =================================================
# INSTRUÇÕES DE SEGURANÇA:
# =================================================
# 1. NUNCA compartilhe este arquivo com senhas reais
# 2. Adicione .env no .gitignore
# 3. Use senhas fortes (12+ caracteres, números, símbolos)
# 4. Rotacione as chaves regularmente
# 5. Use diferentes senhas para cada serviço
# 6. Em produção, considere usar secrets do Docker/Kubernetes
# =================================================