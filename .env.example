# ================================
# QUERO FRETES - Variáveis de Ambiente (2025)
# ================================
# Copie este arquivo para .env e preencha com seus valores
# Comando: cp .env.example .env

# === DATABASE ===
POSTGRES_DB=querofretes_prod
POSTGRES_USER=querofretes_user
POSTGRES_PASSWORD=ALTERE_ESTA_SENHA_FORTE_AQUI
DATABASE_URL=postgresql://querofretes_user:ALTERE_ESTA_SENHA_FORTE_AQUI@postgres:5432/querofretes_prod

# Variáveis PG para compatibilidade
PGHOST=postgres
PGPORT=5432
PGUSER=querofretes_user
PGPASSWORD=ALTERE_ESTA_SENHA_FORTE_AQUI
PGDATABASE=querofretes_prod

# === NODE ===
NODE_ENV=production
PORT=5000
# Gere com: openssl rand -base64 32
SESSION_SECRET=GERE_UMA_CHAVE_SECRETA_AQUI

# === OPENAI (Obrigatório - AI Assistant "Buzino") ===
# Obtenha em: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-proj-xxxxxxxxxxxxxxxxxx

# === OPENPIX (Pagamentos PIX) ===
# Obtenha em: https://openpix.com.br
OPENPIX_APP_ID=sua_chave_openpix
OPENPIX_AUTHORIZATION=seu_token_openpix

# === EMAIL (Configure um provedor) ===
# Opção 1: Gmail (precisa criar senha de app)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=seu.email@gmail.com
SMTP_PASS=senha_de_app_do_gmail

# Opção 2: Hostinger
EMAIL_HOST=smtp.hostinger.com
EMAIL_PORT=465
EMAIL_USER=seu@dominio.com
EMAIL_PASS=sua_senha

# Opção 3: SendGrid
# EMAIL_HOST=smtp.sendgrid.net
# EMAIL_PORT=587
# EMAIL_USER=apikey
# EMAIL_PASS=SG.xxxxxxxxxxxxxxxxxx

# === GOOGLE ANALYTICS (Opcional) ===
# Obtenha em: https://analytics.google.com
VITE_GA_MEASUREMENT_ID=G-XXXXXXXXXX

# === N8N AUTOMATION (Opcional) ===
# Configure seu workflow N8N e cole a URL do webhook
N8N_WEBHOOK_URL=https://sua-instancia.n8n.cloud/webhook/usuario

# === DOMÍNIO (Atualize com seu domínio) ===
DOMAIN=seudominio.com.br
APP_URL=https://seudominio.com.br

# =================================================
# INSTRUÇÕES DE SEGURANÇA:
# =================================================
# 1. NUNCA compartilhe este arquivo com senhas reais
# 2. Adicione .env no .gitignore
# 3. Use senhas fortes (16+ caracteres, números, símbolos)
# 4. Rotacione as chaves regularmente (a cada 90 dias)
# 5. Use diferentes senhas para cada serviço
# 6. Em produção, considere usar Docker Secrets ou Vault
# =================================================
